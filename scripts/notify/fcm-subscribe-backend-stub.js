// **IMPORTANTE**: Este es un stub. Implementar en tu backend real (Node, Go, Python, etc.)
//
// La suscripción a topics en Firebase requiere un endpoint backend que:
// 1. Reciba el FCM token desde el cliente
// 2. Llame a admin.messaging().subscribeToTopic(token, topic)
// 3. Persista la suscripción en base de datos

// Ejemplo: POST /api/fcm/subscribe
// Body: { token: "...", topic: "mundo-dolphins-news" }
// Response: { success: true }

console.warn('⚠️ FCM topic subscription endpoint not implemented. Local subscriptions only.');

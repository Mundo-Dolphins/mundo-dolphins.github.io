# ---
# This workflow sends notifications to Telegram when a new article or podcast is published.
# Automatically detects new articles (content/noticias/*.md) or podcasts (data/season_*.json) 
# when deployed to production.
# Example messages sent:
# 📰 Nuevo artículo publicado: Dolphins ganan el Super Bowl
# 🔗 https://mundodolphins.es/noticias/victoria-super-bowl/
#
# 🎙️ Nuevo capítulo del podcast publicado: Dolphins Podcast Ep. 1
# 🔗 https://mundodolphins.es/podcast/episodio-1/
#
# Requires TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID secrets configured in the repository.
# ---

name: 🚀 Notify new articles or podcasts to Telegram

on:
  workflow_run:
    workflows: ["Deploy Hugo site to Pages"]
    types:
      - completed

concurrency:
  group: notify-articles-podcasts
  cancel-in-progress: false

jobs:
  notify-content:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: 🔄 Checkout repository
        uses: actions/checkout@v6
        with:
          # Use full history because we diff the deployed commit against its parent.
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: 🔍 Detect and send new articles/podcasts to Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          NOTIFY_COMMIT_SHA: ${{ github.event.workflow_run.head_sha }}
          TELEGRAM_MAX_PODCASTS: ${{ vars.TELEGRAM_MAX_PODCASTS || '1' }}
        run: |
          chmod +x scripts/notify/notify_articles_podcasts.sh
          ./scripts/notify/notify_articles_podcasts.sh